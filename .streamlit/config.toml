# Streamlit Configuration for Deployment

[server]
headless = true
port = 8504
# Leave CORS at default (true) so it stays compatible with enableXsrfProtection (default true).

[browser]
gatherUsageStats = false

