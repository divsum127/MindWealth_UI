venv

# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class

# Local env (API keys) — never commit
.env
.env.*

# Streamlit secrets (never commit — use local file only)
.streamlit/secrets.toml

# Local flagged query/response exports (debug)
chatbot/flagged_pairs/
